apiVersion: apps/v1
kind: Deployment
metadata:
  name: cis-dns-external
  namespace: cis-dns
  labels:
    app: cis-dns-external
spec:
  selector:
    matchLabels:
      app: cis-dns-external
  replicas: 2
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: cis-dns-external
      annotations: {}
    spec:
      containers:
      - name: cis-dns-external
        image: ghcr.io/mike-matera/cis-infrastructure:dns-2022061004
        imagePullPolicy: Always
        env:
          - name: DNS_ROLE
            value: "external"
        resources:
          requests:
            cpu: 100m
            memory: 200Mi
          limits:
            cpu: 1000m
            memory: 400Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cis-dns-internal
  namespace: cis-dns
  labels:
    app: cis-dns-internal
spec:
  selector:
    matchLabels:
      app: cis-dns-internal
  replicas: 2
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: cis-dns-internal
      annotations: {}
    spec:
      containers:
      - name: cis-dns-internal
        image: ghcr.io/mike-matera/cis-infrastructure:dns-2022061004
        imagePullPolicy: Always
        env:
          - name: DNS_ROLE
            value: "internal"
        resources:
          requests:
            cpu: 100m
            memory: 200Mi
          limits:
            cpu: 1000m
            memory: 400Mi
